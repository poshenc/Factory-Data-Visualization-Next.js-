
// const formatRagularExpression = {
//     enumerateFormat:
//     expressionFormat:
// }

export function convertOutput(input: string) {
    let result : number[] = []
  

   
    const matchArray = input.match( /\[\d+(\.\d+)?,\d+(\.\d+)?,\d+(\.\d+)?\]/g);

    return result
}